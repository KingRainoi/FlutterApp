import 'package:flutter/material.dart';

//Dark theme colors
const Color darkThemeBgColor = Color.fromARGB(0, 23, 22, 27);
const Color darkThemeItemsColor = Color.fromARGB(124, 56, 53, 65);
const Color darkThemeTextColor = Colors.white;

//Light theme colores
const Color lightThemeBgColor = Color.fromARGB(255, 255, 255, 255);
const Color lightThemeItemsColor = Color.fromARGB(196, 250, 201, 201);
const Color lightThemeTextColor = Colors.black;

// Colores principales
const Color primaryColor = Color.fromARGB(255, 121, 71, 230);
const Color secondaryColor = Color.fromARGB(255, 244, 54, 168);

// Colores secundarios
const Color buttonPrimaryColor = Color.fromARGB(255, 3, 91, 244);
const Color buttonSecondaryColor = Color.fromARGB(161, 11, 126, 235);

// Colores de error
const Color errorColor = Colors.redAccent;
const Color errorTextColor = Colors.white;

// Colores de éxito
const Color successColor = Colors.greenAccent;
const Color successTextColor = Colors.white;

// Otros colores
const Color unselectedItems = Colors.grey;
